import {
  getValue,
  setValue,
  deleteValue,
  listValues,
  addValueChangeListener,
} from "../lib/index.js"

console.log(
  "test",
  getValue,
  setValue,
  deleteValue,
  listValues,
  addValueChangeListener
)
